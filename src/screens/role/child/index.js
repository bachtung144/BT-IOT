import React, {useState} from 'react';
import {View, Text} from 'react-native';
import {useSelector} from "react-redux";

export const ChildUser = () => {
    return (
        <View>
            <Text>tai khoan child</Text>
        </View>
    );
};
