import React, {useState} from 'react';
import {View, Text} from 'react-native';
import {useSelector} from "react-redux";

export const RootUser = () => {
    return (
        <View>
            <Text>tai khoan root</Text>
        </View>
    );
};
