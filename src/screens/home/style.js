import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  ctnPicker:{
    borderWidth: 1,
    height: 50,
    width: 150,
    alignSelf: 'flex-end',
    marginTop: 20,
    backgroundColor: 'white',
  }
});
