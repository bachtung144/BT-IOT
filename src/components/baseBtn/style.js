import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    height: 60,
    backgroundColor: '#65AAEA',
    marginHorizontal: 10,
    marginTop: '30%',
    borderRadius: 30,
    alignItems: 'center',
    justifyContent: 'center',
  },
  txtTitle: {fontSize: 18, color: '#FFFFFF', fontWeight: 'bold'},
});
